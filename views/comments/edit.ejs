<% include ../partials/header %>

<div class="ui main text container segment" style="margin-top: 100px;">
    <div class="ui huge header">Edit <%=product.comment %></div>
    <form class="ui form" action="/products/<%= product._id %>/comments/<%=comment._id %>?_method=PUT" method="POST">
        <div class="field">
            <input type="text" name="comment[text]" value="<%= comment.text %>">
        </div>
<!--        <div class="field">
            <input type="text" name="comment[author]" placeholder="author">
        </div>-->
        <div class="ui buttons" style="margin-left:470px;">
            <a class="ui large grey button" href="/products/<%= product._id %>">cancel</a> 
            <div class="or"></div>
            <button class="ui large green button">update</button>
        </div>
    </form>
</div>

<% include ../partials/footer %>
